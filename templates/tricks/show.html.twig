{% extends "base.html.twig" %}

{% block trick %}

    {% set image = trick.images|first %}
    {% if image %}
        {% set cover = image.name %}
    {% else %}
        {% set cover = 'me-622e7cad5b1e1.jpg' %}
    {% endif %}

    <div class="container" style="z-index: 1500;">
        <div class="card mb-3">
            <img style="height: 18rem;" src="{{ asset('uploads/tricks/' ~ cover) }}" class="card-img-top" alt="Image à la une">
            <div class="card-body">
              <h5 class="card-title">Card title</h5>
              <p class="card-text">This is a wider card with supporting text below as a natural lead-in to additional content. This content is a little bit longer.</p>
              <p class="card-text"><small class="text-muted">Last updated 3 mins ago</small></p>
            </div>
        </div>
    </div>
{% endblock %}

{% block body %}
    <div class="container">
        
    </div>
{% endblock %}